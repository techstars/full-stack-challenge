<div class="container p-5 m-5">
  <div class="row">
    <div class="col md-6">
    <h2>Create a New Company</h2>
        <%= form_for @company do |f| %>
        <div class="form-group">
          <%= f.label :company_name %>
          <%= f.text_field :name, class: "form-control" %>
        </div>
        <div class="row">
            <div class="form-group  mx-sm-3 mb-2">
              <%= f.label :city %>
              <%= f.text_area :city, class: "form-control" %>
            </div>
            <div class="form-group mx-sm-3 mb-2">
              <%= f.label :state %>
              <%= f.text_area :state, class: "form-control" %>
            </div>

            <div class="form-group">
              <%= f.label :founded_date %>
              <%= f.text_field :founded_date, class: "form-control", id: "date-picker" %>
            </div>


        </div>

        <div class="form-group">
          <%= f.label :description %>
          <%= f.text_area :description, cols:100, rows:10, class: "form-control" %>
        </div>





        <%= f.submit "Save", class: "btn btn-primary"%>

        <% end %>

    </div>

  </div>

</div>

