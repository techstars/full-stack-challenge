<%= form_with(model: founder, local: true) do |form| %>
  <% if founder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(founder.errors.count, "error") %> prohibited this founder from being saved:</h2>

      <ul>
      <% founder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :businessid %>
    <%= form.number_field :businessid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
