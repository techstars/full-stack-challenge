import { createStyles, makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => createStyles({
  location: {
    marginTop: 'auto',
    marginBottom: 'auto',
    marginRight: 'auto',
    marginLeft: 10,
  },
  addButton: {
    marginBottom: 15,
    marginTop: 10,
  },
}));